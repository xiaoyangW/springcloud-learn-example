
spring:
  application:
    name: micoserice-feign
server:
   port: 8050
eureka:
  client:
    healthcheck:
        enabled: true
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id:

