spring:
  rabbitmq: #添加rabbitmq配置
    host: 192.168.146.128
    port: 5672
    username: admin
    password: admin
  cloud:
    config:
      name: config-client
      uri: http://localhost:9001/ #配置中心的具体位置
      profile: dev
      label: master
management:
  security:
    enabled: false #取消权限拦截 默认开启
